from typing import List, Tuple

class Solution:
    def minTaps(self, n: int, ranges: List[int]) -> int:
        result:int = 0
        return result
    

sol:Solution = Solution()
n: int = 0
ranges: List[int] = []
print(sol.minTaps(n,range))
